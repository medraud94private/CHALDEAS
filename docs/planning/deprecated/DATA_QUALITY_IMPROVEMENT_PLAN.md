# CHALDEAS 데이터 품질 개선 종합 계획

**Date**: 2026-01-13
**Updated**: 2026-01-14 (Wikidata 매칭 진행 상황 업데이트)
**Status**: Active - Phase 1 진행 중
**Author**: Claude + User

---

## 0. 현재 진행 상황 (2026-01-14)

### 완료된 작업

| 작업 | 상태 | 결과 |
|------|------|------|
| 노이즈 필터링 | ✅ 완료 | 18,493명 noise 분류 |
| data_quality 분류 | ✅ 완료 | verified/sourced/extracted/noise |
| Wikidata 순방향 매칭 (1차) | ✅ 완료 | 23,954명 wikidata_id 연결 |
| 역방향 수집 (philosophers) | ✅ 완료 | 500명 수집 |
| 역방향 수집 (rulers) | ✅ 완료 | 1,000명 수집 |
| 역방향 수집 (military) | ✅ 완료 | 1,000명 수집 |
| 역방향 수집 (scientists) | ✅ 완료 | 1,000명 수집 |

### 진행 중인 작업

| 작업 | 상태 | 비고 |
|------|------|------|
| 순방향 매칭 (추가 배치) | 🔄 실행 중 | 20,000명 처리 중 |
| 역방향 결과 DB 적용 | ⏳ 대기 | 1,432명 매칭 + 1,068명 신규 |

### 현재 DB 상태

| 분류 | 건수 | 비율 |
|------|------|------|
| **verified** (Wikidata 검증) | 23,954 | 8.4% |
| **sourced** (출처 명확) | 3,252 | 1.1% |
| **extracted** (NER 추출) | 240,051 | 84.0% |
| **noise** (필터링) | 18,493 | 6.5% |
| **합계** | 285,750 | 100% |

---

## 1. 현황 및 문제점

### 1.1 초기 데이터 상태 (작업 전)

| 엔티티 | 총 건수 | 문제점 |
|--------|---------|--------|
| Persons | 285,750 | 노이즈 18K, 생몰년 없음 210K, **중복 639개** |
| Events | 51,669 | **완전 중복 4,965개 (9.6%)**, 설명 없음 48% |
| Locations | 40,613 | **중복 5,856개 (14.4%)**, 좌표 부정확 |
| Sources | 76,023 | 엔티티와 연결 미흡 |

### 1.2 이벤트 중복 상세 (2026-01-13 분석)

**완전 중복 (같은 제목 + 같은 연도)**
```
French Revolution (1789): 48개
Storming of the Bastille (1789): 29개
Franco-Prussian War (1870): 28개
Congress of Vienna (1814): 23개
Battle of Waterloo (1815): 22개
...
총 4,965개 삭제 대상
```

**사용자 보고 사례: Babylonian Captivity**
```
[392541] BCE 605 - Babylonian Captivity @ Ešasurra
[347410] BCE 586 - Babylonian Captivity @ Ešasurra  ← 중복
[336738] BCE 586 - Babylonian Captivity @ Ešasurra  ← 중복
```

**같은 사건 다른 이름 (BCE 539 바빌론 정복)**
```
- Cyrus Conquers Babylon
- Cyrus's Stratagem to Take Babylon
- Conquest of Babylon by Cyrus
- Siege of Babylon
- Capture of Babylon (x2)
- Babylon Taken by Cyrus
→ 7개 → 1개로 병합 필요
```

**일반적 이름 (정보 부족)**
```
"Publication": 31개, "Death": 7개, "Coronation": 7개
→ 구체적 이름으로 보강 필요
```

### 1.2 핵심 문제

```
1. NER 추출 노이즈
   - "Mrs. Perry", "Miss Daniel" 같은 무의미한 데이터
   - 285K 인물 중 실제 역사 인물은 일부

2. 데이터 신뢰도 불명확
   - Wikidata 검증 vs NER 추출 구분 없음
   - 출처 추적 불가

3. 중복 데이터
   - 같은 인물이 다른 표기로 중복 존재
   - canonical_id 미활용

4. 관계 데이터 부족
   - 인물-이벤트-장소 연결 약함
   - 소스 문서와 엔티티 연결 없음
```

---

## 2. 개선 전략

### 2.1 투 트랙 접근

```
┌─────────────────────────────────────────────────────────────────┐
│                     Track A: 기존 데이터 정화                     │
│   - 노이즈 필터링 (완료)                                         │
│   - 신뢰도 분류                                                  │
│   - 중복 통합                                                    │
│   - Wikidata 순방향 매칭 (완료: 759명)                           │
└─────────────────────────────────────────────────────────────────┘
                              +
┌─────────────────────────────────────────────────────────────────┐
│                     Track B: 고품질 데이터 확장                   │
│   - Wikidata 역방향 수집 (인물, 이벤트)                          │
│   - Wikipedia 소스 연결                                         │
│   - FactGrid 연동 (선택)                                        │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 신뢰도 체계

```
┌────────────────────────────────────────────────────────────────┐
│                    data_quality ENUM                            │
├────────────────────────────────────────────────────────────────┤
│ VERIFIED    │ Wikidata/FactGrid 검증됨                         │
│             │ - wikidata_id 있음                                │
│             │ - 자동 신뢰                                       │
├────────────────────────────────────────────────────────────────┤
│ SOURCED     │ 출처가 명확함                                     │
│             │ - Wikipedia 문서 연결                             │
│             │ - 생몰년 + mention_count > 5                      │
├────────────────────────────────────────────────────────────────┤
│ EXTRACTED   │ NER로 추출됨 (미검증)                             │
│             │ - 기본값                                          │
│             │ - 추후 검증 필요                                  │
├────────────────────────────────────────────────────────────────┤
│ NOISE       │ 노이즈 데이터                                     │
│             │ - Mrs./Miss/Mr. 패턴                              │
│             │ - API에서 제외                                    │
└────────────────────────────────────────────────────────────────┘
```

### 2.3 소스 타입

```
┌────────────────────────────────────────────────────────────────┐
│                    source_type ENUM                             │
├────────────────────────────────────────────────────────────────┤
│ WIKIDATA    │ Wikidata에서 직접 가져옴                          │
│ WIKIPEDIA   │ Wikipedia 문서에서 추출                          │
│ FACTGRID    │ FactGrid에서 가져옴                               │
│ NER         │ 기존 소스에서 NER로 추출                          │
│ MANUAL      │ 수동 입력                                         │
└────────────────────────────────────────────────────────────────┘
```

---

## 3. DB 스키마 변경

### 3.1 Persons 테이블 확장

```sql
-- 신뢰도 필드
ALTER TABLE persons ADD COLUMN IF NOT EXISTS
    data_quality VARCHAR(20) DEFAULT 'extracted';

ALTER TABLE persons ADD COLUMN IF NOT EXISTS
    source_type VARCHAR(20) DEFAULT 'ner';

ALTER TABLE persons ADD COLUMN IF NOT EXISTS
    verified_at TIMESTAMP;

-- 인덱스
CREATE INDEX IF NOT EXISTS idx_persons_data_quality
    ON persons(data_quality);
CREATE INDEX IF NOT EXISTS idx_persons_wikidata_id
    ON persons(wikidata_id) WHERE wikidata_id IS NOT NULL;
```

### 3.2 Sources 관계 테이블

```sql
-- 소스 테이블 (이미 존재할 수 있음)
CREATE TABLE IF NOT EXISTS sources (
    id SERIAL PRIMARY KEY,
    title VARCHAR(500) NOT NULL,
    source_type VARCHAR(50),      -- wikipedia, wikidata, book, article
    url VARCHAR(1000),
    wikidata_id VARCHAR(50),
    language VARCHAR(10) DEFAULT 'en',
    created_at TIMESTAMP DEFAULT NOW()
);

-- 인물-소스 연결
CREATE TABLE IF NOT EXISTS person_sources (
    person_id INT REFERENCES persons(id) ON DELETE CASCADE,
    source_id INT REFERENCES sources(id) ON DELETE CASCADE,
    citation_context TEXT,         -- 인용 맥락
    PRIMARY KEY (person_id, source_id)
);

-- 이벤트-소스 연결
CREATE TABLE IF NOT EXISTS event_sources (
    event_id INT REFERENCES events(id) ON DELETE CASCADE,
    source_id INT REFERENCES sources(id) ON DELETE CASCADE,
    citation_context TEXT,
    PRIMARY KEY (event_id, source_id)
);
```

### 3.3 Events 테이블 확장

```sql
ALTER TABLE events ADD COLUMN IF NOT EXISTS
    data_quality VARCHAR(20) DEFAULT 'extracted';

ALTER TABLE events ADD COLUMN IF NOT EXISTS
    wikidata_id VARCHAR(50);
```

---

## 4. 데이터 흐름

### 4.1 Wikidata 역방향 수집 파이프라인

```
┌─────────────────────────────────────────────────────────────────┐
│ Phase 1: 인물 수집                                               │
│                                                                  │
│   Wikidata SPARQL ──→ 역사적 인물 (카테고리별)                   │
│     - philosophers, rulers, military, artists, religious...     │
│     - 1900년 이전 출생자                                         │
│     - 예상: ~50,000명                                            │
│                                                                  │
│   출력: poc/data/wikidata_{category}.json                       │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ Phase 2: DB 매칭                                                 │
│                                                                  │
│   Wikidata 인물 vs 기존 DB                                       │
│                                                                  │
│   ┌─────────────┬────────────────────────────────────────────┐  │
│   │ 매칭됨      │ wikidata_id 연결                           │  │
│   │ (score≥70) │ data_quality = 'verified'                  │  │
│   │             │ source_type = 'wikidata'                   │  │
│   ├─────────────┼────────────────────────────────────────────┤  │
│   │ 미매칭      │ 신규 인물로 추가 (선택적)                   │  │
│   │             │ 또는 별도 테이블에 보관                     │  │
│   ├─────────────┼────────────────────────────────────────────┤  │
│   │ 중복 감지   │ canonical_id 설정                          │  │
│   │             │ 대표 인물에 wikidata_id 연결               │  │
│   └─────────────┴────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ Phase 3: Wikipedia 소스 연결                                     │
│                                                                  │
│   각 인물의 Wikipedia sitelinks 가져오기                         │
│     - en.wikipedia.org/wiki/Aristotle                           │
│     - ko.wikipedia.org/wiki/아리스토텔레스                       │
│                                                                  │
│   sources 테이블에 저장 → person_sources 연결                    │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ Phase 4: 이벤트 수집                                             │
│                                                                  │
│   인물 관련 이벤트 가져오기 (P1344, P793)                         │
│     - P1344: participant in (전투, 조약 등)                      │
│     - P793: significant event (재판, 처형 등)                    │
│                                                                  │
│   events 테이블 매칭/추가 → person_events 연결                   │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ Phase 5: 이벤트 소스 연결                                        │
│                                                                  │
│   이벤트의 Wikipedia 문서 가져오기                               │
│   event_sources 연결                                             │
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 기존 데이터 정화 파이프라인

```
┌─────────────────────────────────────────────────────────────────┐
│ Step 1: 노이즈 필터링 (완료)                                     │
│                                                                  │
│   - Mrs./Miss/Mr./Sig. 패턴 → API에서 제외                       │
│   - 18,531명 필터링됨                                            │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ Step 2: 자동 품질 분류                                           │
│                                                                  │
│   def classify_quality(person):                                  │
│       if person.wikidata_id:                                     │
│           return 'verified'                                      │
│       if is_noise_pattern(person.name):                          │
│           return 'noise'                                         │
│       if has_lifespan(person) and person.mention_count > 5:      │
│           return 'sourced'                                       │
│       return 'extracted'                                         │
│                                                                  │
│   예상 분포:                                                     │
│     - verified: ~50,000 (Wikidata 확장 후)                       │
│     - sourced: ~60,000                                           │
│     - extracted: ~160,000                                        │
│     - noise: ~18,000                                             │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ Step 3: 중복 통합                                                │
│                                                                  │
│   Wikidata QID 기준 중복 감지                                    │
│                                                                  │
│   예: "Aristotle" (Q868), "아리스토텔레스" (Q868 언급)            │
│       → canonical_id로 통합                                      │
│                                                                  │
│   통합 전략:                                                     │
│     1. wikidata_id 있는 쪽이 canonical                           │
│     2. 정보가 많은 쪽이 canonical                                │
│     3. 나머지는 canonical_id 참조                                │
└─────────────────────────────────────────────────────────────────┘
```

---

## 5. 구현 스크립트

### 5.1 완료된 스크립트

| 스크립트 | 용도 | 상태 |
|---------|------|------|
| `fetch_wikidata_persons.py` | Wikidata에서 인물 수집 | ✅ 완료 |
| `match_wikidata_to_db.py` | DB 매칭 | ✅ 완료 |
| `apply_wikidata_matches.py` | 매칭 결과 적용 | ✅ 완료 |

### 5.2 필요한 스크립트

| 스크립트 | 용도 | 우선순위 |
|---------|------|----------|
| `update_person_quality.py` | 품질 분류 일괄 적용 | 높음 |
| `fetch_wikipedia_sources.py` | Wikipedia 소스 수집 | 높음 |
| `fetch_wikidata_events.py` | 이벤트 수집 | 중간 |
| `merge_duplicate_persons.py` | 중복 통합 | 중간 |
| `link_persons_to_sources.py` | 소스 연결 | 중간 |

---

## 6. 카테고리별 수집 계획

### 6.1 인물 카테고리

| 카테고리 | Wikidata QID | 예상 인원 | 우선순위 |
|----------|--------------|----------|----------|
| Philosophers | Q4964182 | ~5,000 | ✅ 테스트 완료 |
| Monarchs/Rulers | Q116 | ~20,000 | 높음 |
| Military | Q47064 | ~15,000 | 높음 |
| Religious figures | Q2259532 | ~10,000 | 중간 |
| Scientists | Q901 | ~8,000 | 중간 |
| Writers | Q36180 | ~15,000 | 중간 |
| Artists | Q483501 | ~20,000 | 낮음 |

### 6.2 이벤트 카테고리

| 카테고리 | Wikidata QID | 설명 |
|----------|--------------|------|
| Battle | Q178561 | 전투 |
| War | Q198 | 전쟁 |
| Treaty | Q131569 | 조약 |
| Revolution | Q10931 | 혁명 |
| Coronation | Q209715 | 대관식 |
| Trial | Q8016240 | 재판 |

---

## 7. API 변경사항

### 7.1 현재 적용됨

```python
# person_service.py - 노이즈 필터 적용
NOISE_PATTERNS = ["Mrs.%", "Mrs %", "Miss %", "Mr. %", "Mr %", ...]

def get_persons(..., include_noise: bool = False):
    if not include_noise:
        query = query.filter(not_(or_(*noise_filters)))
```

### 7.2 추가 예정

```python
# 신뢰도 필터
def get_persons(..., min_quality: str = None):
    """
    min_quality: 'verified', 'sourced', 'extracted'
    """
    if min_quality == 'verified':
        query = query.filter(Person.data_quality == 'verified')
    elif min_quality == 'sourced':
        query = query.filter(Person.data_quality.in_(['verified', 'sourced']))
    ...

# 소스 포함 조회
def get_person_with_sources(person_id: int):
    """인물 + 연결된 소스 문서 반환"""
    ...
```

---

## 8. UI 표시 개선

### 8.1 신뢰도 뱃지

```
┌─────────────────────────────────────────────────────────┐
│  아리스토텔레스                           [✓ Verified]  │
│  Aristotle                                              │
│  384 BCE - 322 BCE                                      │
│  Greek philosopher                                       │
│                                                          │
│  Sources: Wikipedia (EN) | Wikipedia (KO) | Wikidata    │
└─────────────────────────────────────────────────────────┘
```

### 8.2 필터 옵션

```
[데이터 품질 필터]
☑ Verified (Wikidata 검증)
☑ Sourced (출처 명확)
☐ Extracted (NER 추출)
☐ Show all
```

---

## 9. 예상 결과

### 9.1 Before vs After vs Current

| 항목 | Before | Current (2026-01-14) | Target |
|------|--------|----------------------|--------|
| wikidata_id 있음 | 98 (0.03%) | **23,954 (8.4%)** | ~50,000 (18%) |
| 신뢰도 분류 | 없음 | **전체 적용** ✅ | 전체 적용 |
| 소스 연결 | 없음 | 0건 | ~50,000건 |
| 이벤트 연결 | 약함 | 약함 | 강화됨 |
| 노이즈 | 혼재 | **분리됨** ✅ | 분리됨 |

### 9.2 데이터 품질 분포

**현재 (2026-01-14)**
```
Total: 285,750
├── Verified (Wikidata): 23,954 (8.4%)  ← 목표의 48%
├── Sourced (출처명확): 3,252 (1.1%)
├── Extracted (미검증): 240,051 (84.0%)
└── Noise (필터링): 18,493 (6.5%)
```

**목표**
```
Total: 285,750+
├── Verified (Wikidata): ~50,000 (17.5%)
├── Sourced (출처명확): ~60,000 (21.0%)
├── Extracted (미검증): ~157,000 (55.0%)
└── Noise (필터링): ~18,750 (6.5%)
```

---

## 10. 실행 계획

### Phase 0: 이벤트 중복 정리 (보류)

- [ ] `deduplicate_events.py` 작성
- [ ] 완전 중복 4,965개 제거 (dry-run 먼저)
- [ ] 일반 이름 136개 보강 검토
- [ ] 유사 사건 병합 후보 생성

### Phase 1: 인프라 ✅ 완료

- [x] DB 마이그레이션 (data_quality, source_type 컬럼)
- [x] sources, person_sources, event_sources 테이블 생성
- [x] update_person_quality.py 실행 (기존 데이터 분류)

### Phase 2: Wikidata 인물 확장 🔄 진행 중

**순방향 매칭 (기존 DB → Wikidata)**
- [x] 1차 배치 완료: 23,954명 wikidata_id 연결
- [ ] 2차 배치 진행 중: 20,000명 처리 중
- [ ] 전체 완료 목표: ~50,000명

**역방향 수집 (Wikidata → DB)**
- [x] philosophers 카테고리: 500명 수집
- [x] rulers 카테고리: 1,000명 수집
- [x] military 카테고리: 1,000명 수집
- [x] scientists 카테고리: 1,000명 수집
- [ ] religious 카테고리: 미수집
- [ ] 역방향 결과 DB 적용: 1,432명 매칭 + 1,068명 신규 추가 대기

### Phase 3: 소스 연결 (대기)

- [ ] fetch_wikipedia_sources.py 작성
- [ ] Wikipedia sitelinks 수집
- [ ] person_sources 연결

### Phase 4: 이벤트 확장 (4-5주차)

- [ ] fetch_wikidata_events.py 작성
- [ ] 인물 관련 이벤트 수집
- [ ] event_sources 연결

### Phase 5: 정제 & 통합 (5-6주차)

- [ ] 중복 인물 통합 (canonical_id)
- [ ] UI 신뢰도 표시
- [ ] 문서화

---

## 11. 관련 문서

| 문서 | 내용 |
|------|------|
| `WIKIDATA_FACTGRID_EXPANSION.md` | Wikidata 역방향 확장 상세 |
| `DATA_PIPELINE_V2.md` | NER 파이프라인 설계 |
| `WIKIDATA_PIPELINE.md` | 순방향 매칭 (기존) |
